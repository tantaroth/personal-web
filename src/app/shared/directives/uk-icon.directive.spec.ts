import { UkIconDirective } from './uk-icon.directive';

describe('UkIconDirective', () => {
  it('should create an instance', () => {
    const directive = new UkIconDirective();
    expect(directive).toBeTruthy();
  });
});
